# Client (exposed to the browser, safe to be public)
VITE_TURNSTILE_SITE_KEY=

# Worker secrets/vars live in .dev.vars (local) and `wrangler secret put` / wrangler.jsonc "vars" (deployed).
# See .dev.vars.example.
